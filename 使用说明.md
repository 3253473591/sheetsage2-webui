# SheetSage2 扒谱 · 使用说明

把一首歌的录音丢进去，自动扒出**主旋律 / 器乐 / 和弦 / 节拍 / 调性 / 段落**，
导出成 **Synthesizer V 工程（SVP 153）/ MIDI**；在**钢琴卷帘**上直接改音符与歌词，离线试听。

> 这是**测试版**。扒谱质量、和弦准确度、SVP 在各版本 SynthV 里的兼容性都还在验证，
> 欢迎把「哪首歌、什么现象、生成的产物」反馈回来。

---

## 0. 先看这里：你是哪种包

| 包名 | 大小 | 怎么开始 |
| --- | --- | --- |
| `...-full.zip` **全量整合包** | 压缩包约 4.4 GB（解压后 6.0 GB） | 解压 → 双击 **`start.bat`**。**不需要装任何东西**，断网也能跑。 |
| `...-lite.zip` **轻量包** | 压缩包约 120 MB（解压后 330 MB） | 解压 → 双击 **`start.bat`**。第一次要联网下载约 3 GB 环境（Python/torch/模型），之后就能离线用。 |

两个包**用法完全一样**，区别只是「环境是随包给的」还是「第一次联网下载的」。

> **解压注意**：请**整个文件夹解压出来**再运行，不要在压缩软件里双击里面的文件。
> 路径里可以有中文和空格，但**不要放在 OneDrive / 网盘的同步目录里**（会导致文件被占住）。

---

## 1. 硬件要求

| 项 | 要求 |
| --- | --- |
| 系统 | Windows 10 / 11，64 位 |
| 显卡 | **非必需**。有 NVIDIA 显卡（显存 ≥ 4 GB）最快；没有也能跑，走 CPU |
| 速度参考 | RTX 3080 12G：5 分钟的 FullSong 约 **1 分钟**；纯 CPU 大约 **15–40 分钟**（看核心数） |
| 磁盘 | 全量包解压后约 6 GB；轻量包初始 330 MB，装完约 3.5 GB |
| 网络 | 全量包完全不需要；轻量包只有第一次安装需要（约 3 GB） |

**没有显卡怎么办？** 什么都不用改。程序启动时会自己判定：
能上 GPU 就上 GPU，上不了（没卡 / 驱动太老 / 显存被占满）就自动退回 CPU，
并在「环境」面板里写清楚当前用的是哪个。

想**强制**用 CPU（比如显卡同时要打游戏）：用记事本打开 `settings.json`，
把 `"default_device": "auto"` 改成 `"cpu"`，保存后重开 `start.bat`。

### 软件环境要求（都是随包给的，一般不用管）

| 项 | 要求 | 说明 |
| --- | --- | --- |
| 系统 | **Windows 10 / 11，64 位** | 只有 Windows x64 版，Linux/macOS 不行 |
| Python | **3.12**（随包 3.12.11） | 自带的便携版，不用你装 |
| **PyTorch** | **≥ 2.2（硬下限）**，**建议 ≥ 2.6** | 两个包默认都装 **2.10.0** |
| torchaudio | **必须与 torch 版本完全一致** | 不一致必崩 |
| 其他依赖 | `requirements-base.txt` 里 69 项 | 随包安装 |

**为什么下限是 2.2**（三条独立约束，取最大者）：

* `transformers 4.57.6` 自己声明 `torch>=2.2`；
* `accelerate 1.15.0` 声明 `torch>=2.0.0`；
* 便携运行时是 **CPython 3.12**，而 torch 从 **2.2.0** 起才提供 cp312 的 Windows wheel
  （已核实：`2.2.0` 的轮子存在，`2.1.2` 的没有）。

再补一条实地经验：numpy 钉在 2.5，而 **torch < 2.3 与 numpy 2.x 有已知的 C-API 不兼容**，
所以真要用老版本，建议不要低于 **2.6**。

> **实测跑通的**：`torch 2.6.0+cpu` 与 `torch 2.10.0`（cu130，GPU 与 CPU 都跑过）。
> 2.2–2.5 只是按声明推算、**没有实测**，属于"理论上可以"。

### 我想换别的 torch 版本

程序里**没有任何 torch 版本断言**，换版本不会报"不支持"。两条路：

**A. 装的时候指定**（轻量包，推荐）

```bat
:: 换个版本（本地标签 +cpu/+cu124 会自动按 --torch-index 推导）
runtime\python\python.exe tools\install_deps.py --torch-version 2.6.0 ^
    --torch-index https://download.pytorch.org/whl/cpu

:: 或者完全自定义要装哪两个包
runtime\python\python.exe tools\install_deps.py ^
    --torch-spec "torch==2.6.0+cpu,torchaudio==2.6.0+cpu" ^
    --torch-index https://download.pytorch.org/whl/cpu
```

**B. 装完以后再换**（全量包/轻量包都适用）

```bat
runtime\python\python.exe -m pip install torch==2.6.0+cpu torchaudio==2.6.0+cpu ^
    --index-url https://download.pytorch.org/whl/cpu
```

⚠️ **`cu130` 源里只有 torch ≥ 2.9**。想装更老的 CUDA 版，得同时换源，例如
`--torch-index https://download.pytorch.org/whl/cu124`、`.../cu126`、`.../cu128`。

换完双击 **`selfcheck.bat`**，「PyTorch」那一行会打印当前实际版本。

---

## 2. 跑起来

1. 双击 **`start.bat`**
2. 会弹出一个黑色命令行窗口（**别关它**，关了服务就停了），浏览器自动打开
   `http://127.0.0.1:8777/`
   窗口里会先把环境摊开给你看：解释器路径、ffmpeg 路径、**以及这次会用 GPU 还是 CPU** ——
   出问题时就照这几行一起发出来。
3. 环境有问题时页面顶部会自动弹红条（写明是哪一项、怎么修）；
   能跑就不会拦你，真跑不了才会拒绝开工。
   底部状态栏左边常显**这次用的是 GPU 还是 CPU**、显存占用多少
4. 按页面上的 ①→⑤ 走：

| 步骤 | 干什么 |
| --- | --- |
| ① 拖入音频 | 拖 `.wav` / `.mp3` / `.flac` 进去（单曲最大 300 MB）。**计算精度**、**只分析前 N 秒**、**开始扒谱 / 停止** 也都在这一张卡上 |
| ② 进度 | 等它跑完；旁边的「分析概览」会给出 BPM、调性、段落、**和弦进行**（可下载 .txt / .csv） |
| ③ 钢琴卷帘 | **改音符与歌词的地方**。竖轴是音高、横轴是时间，双击加音符、拖动改位置、拖边缘改长短、`Delete` 删除；顶部「**导出内容**」有三个勾选：**人声主旋律 / 器乐旋律 / 和弦**。改完点「**保存并重新生成导出**」 |
| ④ （已合并进 ③） | 歌词不再单独一步：在卷帘上选中音符直接输入，或用 `Ctrl+L` 批量填词 |
| ⑤ 导出 | **导出 SVP**（给 Synthesizer V Studio）、**导出 MIDI** |

> **导出什么，你自己勾。** ③ 顶部那三个勾选框：
>
> * **人声主旋律** —— 默认**唯一勾上**的；
> * **器乐旋律**；
> * **和弦**。
>
> **默认只导出「人声主旋律」一条轨。** 想要器乐或和弦，自己把对应的勾点上。
> 勾选决定「卷帘上看得到哪些轨」和「导出哪些轨」，**不需要重跑推理**。
> 点导出时程序会先按当前勾选重新生成，再下载 —— 不会再让你下到上一次的旧文件。
>
> 卷帘读的是**模型给出的全部声部**，不受你扒谱当时勾了什么影响；
> 所以扒完可以随时改主意，把器乐/和弦勾上来看和导。如果某个声部这首歌本来就没有
> （模型没识别出器乐），勾上后卷帘会直接告诉你"乐谱里只有 Vocal"，
> 而不是丢一句没头没尾的空提示。
>
> **关于重叠的音符**：虚拟歌姬（Synthesizer V）一条轨里不允许音符重叠。
> 卷帘在编辑时就会**维持每条轨单声部**（把被压到的后一个音符往右推），
> 所以正常拖动不会拖出重叠。万一还是叠上了，程序会检查**每一条轨**，
> 凡是同时响着多个音的，就**自动拆成 `名字1`、`名字2`、`名字3`…**
> （同一时刻最多几个音同时响，就拆几条），**一个音都不会丢**；
> 本来就不重叠的轨保持原名不变。
>
> 实际用起来：**和弦**本来就是多个音同时响，所以勾上「和弦」后看到
> `和弦1`…`和弦5` 是正常的，不是 bug。
> 而**人声主旋律**和**器乐旋律**是单声部旋律线，正常情况下**不会**被拆；
> 如果你看到 `主人声1`、`主人声2`，那更可能是这首歌的时值/速度识别有问题，
> 可以反馈给我们（见 `ASK_AI.md`）。
>
> **歌词要自己填**：本工具**不做歌词识别**。卷帘上没填词的音符会唱占位词 `la`，
> 所以**填完词再点保存/导出**。

### ③ 钢琴卷帘怎么用

| 想做什么 | 怎么做 |
| --- | --- |
| 加音符 | 在空白处**双击**（加在当前轨，默认歌词 `la`） |
| 删音符 | 选中后 `Delete` |
| 改位置 / 改长短 | 拖动音符本体 / 拖它的左、右边缘 |
| 选多个 | 空白处**拖框**；`Shift+点` 追加；`Ctrl+A` 全选当前轨 |
| 对齐网格（吸附） | `Ctrl+G` 开关吸附；网格档位在下拉框里选：四分音符 / 8 分 / **16 分（默认）** / 16 分三连音 / 32 分 / 32 分三连音 |
| 缩放时间轴 | `Ctrl+滚轮`（以鼠标位置为中心）；工具栏 `＋` `−` /「适应宽度」 |
| 定位试听位置 | 在卷帘空白处**点一下**，或点顶部标尺 |
| 播放 / 暂停 | **空格**（用内置大钢琴音色离线试听） |
| 撤销 / 重做 | `Ctrl+Z` / `Ctrl+Shift+Z`（或 `Ctrl+Y`） |
| 改单个音的歌词 | 选中音符，在「歌词」输入框里打字（中文直接用输入法）；`Tab` 跳到下一个音符 |
| 批量填词 | 选好音符 → `Ctrl+L` → 粘贴歌词 → 勾不勾「按字符隔开」见下 |
| 跟随走带位置 | 播放时视图自动跟着滚动。工具栏「跟随」按钮或按 `F` 开关 |
| 滚动 | 横向、纵向都有滚动条，也可以直接拖动 |

> **音高范围固定是 C0–C8**（整架钢琴），所以纵向滚动条**一直都在**，
> 想往上写高音直接拖滚动条或往上一路拖音符就行，不用先拖到顶、松手、等它扩范围。
> 打开卷帘时会自动滚到你这首歌实际用到的音区。
>
> **拖动音符**：左右拖改位置、上下拖改音高、拖左右边缘改时值。
> 想让它跨过旁边那个音符，就**拖它就够了**——被拖的那个说了算：
> 往左拖会**占掉前一个音符的尾巴**（前一个收短），往右拖则把后一个整体推开（时值不变）。
> 一次拖动 = 一步撤销，`Ctrl+Z` 一下就回到拖之前。
>
> **同一轨里同时响的音**（模型给的 `[CEG]` 和弦写法）不会被自动拆开，
> 打开卷帘时保持原样；导出时程序会按「一个音都不丢」的规则拆成 `和弦1/和弦2…`。

> **`Ctrl+L` 填词框里的「按字符隔开」**：
> 勾上 = **一字一音**（`我爱你` → 我 / 爱 / 你），会把空格和换行都去掉；
> 不勾 = **按空格切词**（`wo ai ni` → wo / ai / ni，适合拼音或英文）。
> 词比音符少时，**多出来的音符保持原样**（不会把已经填好的词冲掉）；
> 词比音符多时多的会被忽略，并告诉你忽略了几个。
>
> **填词从哪儿开始**：**没选中音符** → 从当前轨的第一个音符开始铺；
> **框选了若干音符** → 从**框选到的第一个**（时间最早的）开始铺，前面的音符不动。
> 所以想只改副歌那一句，就先把那句框起来再按 `Ctrl+L`。
>
> **框选之后拖动，选中的音符会整组一起动**（音高、位置都一起），
> Shift+点可以加选 / 减选。
>
> **改动会自动保存**：最后一次改动停下约 0.3 秒后，程序自动保存并重新生成导出
> （③ 顶部的橙色「有未保存的改动」会随之消失）。也可以随时点
> 「保存并重新生成导出」手动存一次；想丢掉改动就点「放弃改动，重新加载」。
>
> **改动会自动保存**（停下约 0.3 秒后自动落盘并重生成导出），
> 所以不用怕忘记点保存；有还没存上的改动时，③ 顶部会出现橙色的「有未保存的改动」。

### 底部状态栏（右边两个按钮）

| 元素 | 作用 |
| --- | --- |
| `#任务号` | 当前任务编号；反馈问题时连带它一起发 |
| **清理缓存** | 清空 `output\`（历史任务产物）与 `tmp\`（上传的音频 + 中间文件） |
| **日志** | 展开 / 收起运行日志 |

**清理缓存怎么用**：点一下 → 先算出两个目录各占多少（几个文件 / 多大）→
弹确认框 → 点确定才动手。扒谱进行中按钮是灰的（跑任务时不让删）。

> ⚠️ **这是永久删除，不进回收站，删掉找不回来。** 想留的产物（`.svp` / `.mid` / `roll.json`）
> 先自己拷到别处。两个目录**本身会保留**，下次启动和上传时会自动重建。
> 磁盘紧张、或者攒了几十次测试任务时用它清一下最省事。

### 端口被占用
双击两次 `start.bat` 不会出错：第二次会认出「已经在跑」，直接把浏览器指过去。
如果是**别的程序**占了 8777，用记事本改 `settings.json` 里的 `"port"`，或
把 `start.bat` 复制一份、右键「编辑」在后面加 ` --port 8888`。

---

## 3. 产物在哪、怎么用

```
output/<日期时间>_<任务号>/
    export/<歌名>.svp        ← 给 Synthesizer V Studio（格式 version 153）
    export/<歌名>.mid        ← 通用 MIDI
    roll.json                ← 卷帘上你的音符与歌词（③ 的编辑结果）
    score.abc                ← 模型输出的原始乐谱（内部格式，不再作为产物提供）
    transcription.mid        ← 模型原始多轨（人声 / 器乐 / 和弦）
    chords.mid / melody_*.mid / beat.lab / chord.lab / key.lab / structure.lab
    summary.json             ← 这一次的所有参数、耗时、诊断
```

* **SVP**：直接拖进 Synthesizer V Studio 打开即可（已在真实 SynthV 上验证能打开）。
* **MIDI**：任何 DAW 都能开。
* **summary.json**：反馈问题时**请把它一起发出来**，里面有设备、精度、耗时、诊断。

---

## 4. 出问题了怎么办（三步）

1. **双击 `selfcheck.bat`** —— 它会在当前目录生成一份 `环境快照.txt`
2. 打开 `ASK_AI.md`，把里面的模板 + `环境快照.txt` 整个粘给任意 AI
   （ChatGPT / Claude / DeepSeek / 豆包 都行）
3. 如果 AI 让你改东西，改完再跑一次 `selfcheck.bat` 对照

**90% 的问题自己就能好**：
* 报「端口被占用」→ 改 `settings.json` 的 `port`
* 报「找不到权重 / 缺 model.safetensors」→ 全量包是解压不完整，重新解压；
  轻量包是模型没下完，再双击一次 `start.bat` 续传
* 报 **`FFmpeg is required to read audio files`** → 包里的
  `runtime\ffmpeg\bin\ffmpeg.exe` 不见了，十有八九是**被杀毒软件删了**（它长得像"未知
  程序"）。把整合包目录加进杀毒白名单后重新解压；实在不行，把环境变量 `FFMPEG_BIN`
  指向任何一个 ffmpeg.exe 也能跑
* 扒谱中途失败 → 看黑窗口最后几行的报错，连同 `环境快照.txt` 一起发出来
* 磁盘被占满 / 攒了一堆测试任务 → 点状态栏的 **清理缓存**（永久删除，先看确认框里的数字）

---

## 5. 隐私 & 安全

* 服务**只监听 `127.0.0.1`**（本机回环），同一局域网的其他人都访问不到。
* 上传的音频放在 `tmp/uploads/`，任务过期会自动清理；想彻底清干净就删掉
  `tmp/` 和 `output/` 两个目录（下次启动会自动重建）。
* 程序**不联网**（全量包；轻量包只有第一次安装联网），不上传你的任何数据。

---

## 6. 目录里都有啥

```
start.bat          启动（就是双击这个）
install.bat        轻量包首次安装用（全量包用不到；双击 start.bat 也会自动调它）
selfcheck.bat      生成环境快照，出问题时用
使用说明.md        本文件
ASK_AI.md          遇到问题把里面的内容粘给 AI
许可与来源.md       各组件/权重的许可证与出处（CC BY-NC 4.0，仅限非商业测试）
版本信息.json       这一包是什么版本、里面装了什么
app/               后端（Python）
web/               前端单页 + 离线乐谱渲染 + 钢琴音色
models/            模型权重（SheetSage2 218 MB + MERT-v2-FullSong 2.4 GB）
runtime/           随包运行时：便携 Python + ffmpeg（+ uv）
tools/             自检与安装脚本
output/  tmp/      运行期产物，可随时删
```

---

## 7. 许可提示（请务必读一眼）

模型权重（SheetSage2、MERT-v2-FullSong）是 **CC BY-NC 4.0：仅限非商业用途**。
拿来测试、学习、自己扒谱没问题；**不要用于商业发行、不要二次售卖**。
详见 `许可与来源.md`。
